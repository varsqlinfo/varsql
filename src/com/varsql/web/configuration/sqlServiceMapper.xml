<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sqlServiceMapper">
	
	<!-- 사용자 sql 로그 저장 -->
	<insert id="sqlServiceMapper.insertSqlUserLog" parameterType="java.util.Map">
		insert into VTSQL_LOG (
			VCONNID
			, UID
			, START_TIME
			, S_MM
			, S_DD
			, S_HH
			, END_TIME
			, DELAY_TIME
			, LOG_SQL
			, RESULT_COUNT
			, COMMAND_TYPE
		)
		values( 
			#{vconnid}
			, #{uid}
			, #{start_time}
			, #{s_mm}
			, #{s_dd}
			, #{s_hh}
			, #{end_time}
			, #{delay_time}
			, #{log_sql}
			, #{result_count}
			, #{command_type}
		)
	</insert>
	
	<!-- 사용자 sql 정보 저장  -->
	<insert id="sqlServiceMapper.saveQueryInfo" parameterType="java.util.Map">
		insert  into VTSQL_BOARD (
			UID
			, VCONNID
			, SQL_ID
			, GUERY_TITLE
			, QUERY_CONT
			, SQL_PARAM
			, REG_DT
			, UPD_DT
		) 
		values(
			#{uid}
			, #{vconnid}
			, #{sql_id}
			, #{sqlTitle}
			, #{sql}
			, #{sqlParam}
			, CURRENT TIMESTAMP
			, CURRENT TIMESTAMP
		)
	</insert>
	
	<!-- 사용자 sql 정보 업데이트  -->
	<update id="sqlServiceMapper.updateQueryInfo" parameterType="java.util.Map">
		update VTSQL_BOARD 
		set
			 GUERY_TITLE = #{sqlTitle}
			, QUERY_CONT = #{sql}
			, UPD_DT = CURRENT TIMESTAMP
			, SQL_PARAM =#{sqlParam}
		where sql_id = #{sql_id}
		and VCONNID = #{vconnid}
		and UID = #{uid}
	</update>
	
	<!-- 사용자 sql 마지막 저장 정보  -->
	<select id="sqlServiceMapper.selectLastSqlInfo" parameterType="map" resultType="map">
		select * from VTSQL_BOARD
		where VCONNID = #{vconnid}
		and UID = #{uid}
		order by upd_dt desc
		FETCH FIRST 1 ROWS ONLY
	</select>
	
	<!-- 사용자 sql 목록 토탈 카운트 -->
	<select id="sqlServiceMapper.selectSqlListTotalCnt" parameterType="map" resultType="int">
		select count(1) as TOTALCNT 
		from VTSQL_BOARD
		where VCONNID = #{vconnid}
		and UID = #{uid}
		and GUERY_TITLE like '%'||#{searchVal}||'%' 
	</select>
	
	<!-- 사용자 sql 목록  -->
	<select id="sqlServiceMapper.selectSqlList" parameterType="map" resultType="map">
		<bind name="pageStartNum" value="(first-1)" />
		select * from VTSQL_BOARD
		where VCONNID = #{vconnid}
		and UID = #{uid}
		and GUERY_TITLE like '%'||#{searchVal}||'%' 
		order by upd_dt desc OFFSET ${pageStartNum} ROWS FETCH FIRST ${rows} ROWS ONLY
	</select>
	
	<!-- sql 저장 정보 삭제 .  -->
	<delete id="sqlServiceMapper.deleteSqlSaveInfo" parameterType="java.util.Map">
		delete from VTSQL_BOARD
		where SQL_ID = #{sql_id}
	</delete>
	
</mapper>
